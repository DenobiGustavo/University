namespace Translator
{
    public enum TokenType
    {
        PLUS,
        MINUS,
        TIMES,
        DIVIDE,
        LPAREN,
        RPAREN,
        ASSIGN,
        EOL,
        EOF,
        NUMBER,
        VAR,
        INPUT,
        OUTPUT,
        ERROR
    }
}
