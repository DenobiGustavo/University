#include <stdio.h.>

int main()
{
int idade_homem, idade_mulher;

prinf("Qual a idade do casal?: ");
scanf("%d%d", &idade_homem, &idade_mulher);

if(idade_homem %2 == 0 && idade_mulher %2 == 0);
printf("Desconto concedido");

}