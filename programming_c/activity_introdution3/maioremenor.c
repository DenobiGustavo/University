#include <stdio.h>

int main()
{
    int n, count, soma;

    printf("Quantos numeros inteiros vc vai escolher: ");
    scanf("%d", &n);

    for(count = 1; count <= n; count++){
        printf("digite um numero: ");
        scanf("%d", numero_escolhido);
        
    }
}